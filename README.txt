Instead of iterating (for_1) to total array elements 
while input array elements 
we add values to  edx
now we have total of the array elements instead of iterating over the array again!!
instruction idiv is confusing because all values are positive so I used div instead





